version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - run:
          name: Test
          command: ./manage.py test
      - python/save-cache

workflows:
  main:
    jobs:
      - build-and-test
